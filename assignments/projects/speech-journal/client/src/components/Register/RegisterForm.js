import React from 'react'

const RegisterForm = () => {
  return (
    <div>
        <form onSubmit={}>
            <input 
                type="text"
                name="username"
                value={} 
                onChange={} />
            <input 
                type="password"
                name="password"
                value={} 
                onChange={} />
            <button>Register</button>
        </form>
    
    </div>
  )
}

export default RegisterForm
