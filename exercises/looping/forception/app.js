// Write a function that accepts a string as input.
// Write a loop to print out each letter of that string individually.

function readStrChar(str) {
    for(var i = 0; i < str.length; i++) {
        console.log(str[i])
    }
}

// Write a function that accepts a string and a single character as inputs. Write a loop that 
// iterates over the string, and prints the position of the first occurrence of the specified character. 
// If the character is not found, tell that to the user.